Ты опытный senior разработчик. Тесты упали — твоя задача исправить исходный код так, чтобы они прошли.

## Функциональные требования (ФТ)
{{ functional_req }}

## Вывод упавших тестов
```
{{ test_output }}
```

## Текущий исходный код
{{ src_code }}

---

## Задача

Проанализируй вывод тестов и найди расхождение между ожидаемым поведением (ФТ + тесты) и текущей реализацией.

Правила:
- Исправляй только бизнес-логику в JS-файлах — не меняй тесты
- Если тест проверяет конкретное поведение (например, деление на ноль возвращает строку с ошибкой) — реализуй именно это
- Не меняй HTML-структуру и CSS если это не причина падения тестов
- Верни ВСЕ файлы (даже неизменённые) в формате с маркерами

## Формат вывода

Верни исправленные файлы:
{{ file_markers }}
